<% @listing = Listing.find(params[:listing_id]) %>

<h1>Reservations for <%= @listing.title %></h1>

<p>
<b>Check In:</b> 
<%= @reservation.check_in %>
</p>
<p>
<b>Check Out:</b> 
<%= @reservation.check_out %>
</p>

<%= link_to "My Profile", user_path(current_user) %>

<%= link_to 'All Listings', listings_path %>